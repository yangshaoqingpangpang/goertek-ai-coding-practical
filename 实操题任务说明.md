# 歌尔 Claude Code 能力测评 · 实操题任务说明

| 项目 | 说明 |
|------|------|
| 测评名称 | Claude Code / AI 编程助手 能力测评 |
| 环节 | 实操题 |
| 分值 | 35 分 |
| 用时 | 见测评通知 |
| 答题方式 | 3 个方向**任选其一**完成 |

---

## 一、总体要求

1. 实操题共 3 个方向（A 功能开发 / B 缺陷修复 / C 代码重构），**任选其一**完成。
2. 全程建议遵循 **计划 → 实施 → 复审 → 验证** 的工作流，并保留关键的对话记录与提交记录，作为评分依据。
3. 评分维度（各方向通用）：

   | 维度 | 分值 | 说明 |
   |------|------|------|
   | 功能完整性 | 8 | 是否完成题目要求的核心功能 |
   | 代码质量 | 8 | 可读性、命名、结构、是否复刻项目既有风格 |
   | Prompt 质量 | 8 | 与 AI 助手交互的指令是否清晰、可复现、上下文充分 |
   | 工具运用 | 6 | 对 AI 编程助手的运用熟练度（上下文管理、迭代纠错等） |
   | 对话效率 | 5 | 是否以尽量少的轮次达成目标，避免无效反复 |

4. 关于代码中 `AI-KEEP` 标注：项目内部分代码段被 `AI-KEEP BEGIN ... AI-KEEP END` 注释包裹。这些是**题目保留内容**，AI 编程助手扫描时会跳过该区间——既不会修改、也不会反馈其内容。**考生不得删除或修改 `AI-KEEP` 标注区间内的代码**（方向 C 的行为测试除外，该测试用于验证你的重构，同样不可修改其中断言）。

---

## 二、方向 A：新增查询接口（功能开发）

**仓库目录**：`实操题代码/eladmin/`（基于 eladmin 2.7，Spring Boot 2.7 + JPA + Vue）

### 任务

在项目中新增一个"候选人查询"REST 接口：

1. 支持按**姓名**、**手机号**分页查询候选人列表；
2. 对入参做校验（分页参数范围、手机号格式）；
3. 校验异常由项目既有异常处理机制统一兜底，返回友好错误信息；
4. 前端新增查询页并与后端联调（如项目含前端）。

### 已准备的骨架（不要改动其中 `AI-KEEP` 标注的占位实现）

| 文件 | 作用 |
|------|------|
| `sql/candidate.sql` | 建 `sys_candidate` 表，含中文测试数据 |
| `modules/candidate/domain/Candidate.java` | 实体类（已含 `@NotBlank` / `@Pattern` 手机号 / `@Email` 校验注解） |
| `modules/candidate/service/CandidateService.java` | 服务接口 |
| `modules/candidate/service/impl/CandidateServiceImpl.java` | 服务实现占位（方法体被 `AI-KEEP` 标注，需在指定位置替换实现） |
| `modules/candidate/rest/CandidateController.java` | 控制器空壳（被 `AI-KEEP` 标注，需补全） |

> 启动前请先执行 `sql/candidate.sql` 建表与导入数据。

### 考生需完成

1. 新建 `CandidateQueryCriteria`（含 `name`、`phone`，参考项目自带的 `JobQueryCriteria`）；
2. 新建 `CandidateDto`（参考 `JobDto`）；
3. 新建 `CandidateMapper`（mapstruct）与 `CandidateRepository`（JpaRepository + JpaSpecificationExecutor）；
4. 实现 `CandidateServiceImpl.queryAll`：按姓名 / 手机号动态分页查询；
5. 实现 `CandidateController.queryCandidate`：入参为 `CandidateQueryCriteria + @Validated`，分页参数范围校验（page≥0、0＜size≤100，越界抛出项目既有的异常类），返回分页结果；
6. 确保校验异常经全局异常处理返回友好提示。

### 交付

可运行代码 + 关键对话 / Prompt 说明。

---

## 三、方向 B：定位并修复 3 个已知缺陷（缺陷修复）

**仓库目录**：`实操题代码/RuoYi-Vue-带bug/`（基于若依 RuoYi-Vue 3.9.2，Spring Boot + MyBatis + Vue）

> ⚠️ **重要提示**：本方向的 3 个缺陷均被 `AI-KEEP` 注释包裹。AI 编程助手扫描到这些代码区间时会直接忽略、不修改、不反馈。因此**本方向考察的是人工定位与修复缺陷的能力**，请勿依赖 AI 直接给出修复方案。你仍然可以使用 AI 协助你编写复现脚本、理解代码逻辑等，但缺陷本身的定位与修复需要由你独立完成。

### 任务

项目可正常编译和启动，但运行时存在 3 个已知缺陷（均满足"可编译、可启动、运行时出错且可复现"）。请逐个：

1. **复现**并用日志 / curl / 断点等手段**定位**（不要凭猜测）；
2. **修复**并说明每个缺陷的根因；
3. **回归验证**，确认修复有效且不引入新问题。

### 缺陷类型提示（仅告知类型，不告知具体位置）

- 1 个**分页参数**相关缺陷
- 1 个**空指针（NPE）**缺陷
- 1 个**时间字段时区 / 格式**相关缺陷

### 交付

修复后的可运行代码 + 每个缺陷的定位方式 / 根因 / 验证说明。

---

## 四、方向 C：耦合代码拆分为清晰分层（代码重构）

**仓库目录**：`实操题代码/mall/`（基于 macrozheng/mall，Spring Boot + MyBatis 电商系统）

### 任务

详细要求见仓库根目录 **`REFACTOR_TASK.md`**，本说明仅作摘要。

将 `OmsPortalOrderServiceImpl.generateOrder(OrderParam)` 方法（约 156 行，混合了参数校验、库存、优惠券、积分、订单落库、消息发送等多类职责）重构为清晰的分层结构。

### 核心要求

1. **对外行为完全不变**——以仓库自带的 `OmsPortalOrderBehaviorTest`（5 个 Mockito 测试用例，被 `AI-KEEP` 包裹，**不可修改断言**）作为客观验证依据，重构后该测试必须 100% 通过；
2. 拆分为职责单一的子方法或独立 Service（命名规范、依赖方向清晰）；
3. 说明重构思路与每步意图。

### 验证命令

```bash
cd 实操题代码/mall
mvn -pl mall-portal test
```

### 交付

重构后代码 + 重构说明（含如何保证行为不变）。

---

## 五、目录结构

```
歌尔实操-交付包/
├── 实操题任务说明.md          ← 本文件
└── 实操题代码/
    ├── eladmin/               ← 方向 A 选用
    ├── RuoYi-Vue-带bug/       ← 方向 B 选用
    └── mall/                  ← 方向 C 选用
```

任选一个方向，打开对应目录即可开始。
